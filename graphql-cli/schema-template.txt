const { gql } = require('apollo-server-express');

module.exports = gql`
  type ${name} {
    ${contentMain}
  }

  extend type Query {
    ${name}(id: ID!): ${name}
    ${name}s(filter: ${name}Filter): [${name}!]
  }

  extend type Mutation {
    create${name}(
      ${contentMainNoId}
    ): ${name}!
    update${name}(
      ${contentNoRequired}
    ): ${name}!
    delete${name}(
      id: ID!
    ): ${name}!
  }

  input ${name}Filter {
    example: String
  }
`;
